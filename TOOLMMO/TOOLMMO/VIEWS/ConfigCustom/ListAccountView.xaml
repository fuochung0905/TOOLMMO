<UserControl x:Class="TOOLMMO.VIEWS.ConfigCustom.ListAccountView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:TOOLMMO.VIEWS.ConfigCustom"
             xmlns:local1="clr-namespace:TOOLMMO.COMMON"
             xmlns:c="clr-namespace:TOOLMMO.VIEWS.BASES.Controls"
             mc:Ignorable="d" 
             Background="White" 
             Width="400">
    <UserControl.Resources>
        <local1:BoolToBrushConverter x:Key="BoolToBrushConverter"/>
    </UserControl.Resources>
    <Grid >
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Chọn user" 
               FontWeight="Bold" FontSize="18" 
               HorizontalAlignment="Center" 
               Background="#2A66EA" 
               Foreground="White" 
               Padding="10" 
               Margin="0,0,0,10"/>

        <ListBox Grid.Row="1" ItemsSource="{Binding Users}" BorderThickness="0">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Border CornerRadius="8"
                    Margin="5"
                    Padding="10"
                    Background="{Binding IsSelected, Converter={StaticResource BoolToBrushConverter}}"> 
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>

                            <Image Source="{Binding Avatar}" Width="40" Height="40" />
                            <StackPanel Grid.Column="1" Margin="10,0" VerticalAlignment="Center">
                                <TextBlock Text="{Binding UserName}" FontWeight="Bold" FontSize="14"/>
                                <TextBlock Text="{Binding Name}" FontSize="12"/>
                            </StackPanel>
                            <CheckBox Grid.Column="2" IsChecked="{Binding IsSelected, Mode=TwoWay}" VerticalAlignment="Center"/>
                        </Grid>
                    </Border>
                </DataTemplate>

            </ListBox.ItemTemplate>
        </ListBox>
        <c:MMOButton Content="Chọn User"
         Height="50"
         FontWeight="Bold"
         Foreground="White"
         Background="#F57C00"
         BorderThickness="0"
         Cursor="Hand"
         Command="{Binding NavigateToConfigScenarioCommand}"  />
    </Grid>

</UserControl>
